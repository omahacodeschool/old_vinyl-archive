<h1><%= areas_show_link(@area) %></h1>

<!-- Cycles through each album associated with a given area -->
<% @area.with_these_albums.each do |album| %>
  <br />
  <%= albums_show_image_link(album) %>
  <br />
  <%= albums_show_link(album) %>
  <br />
  <%= artists_show_link(album.artist) %>
<% end %>