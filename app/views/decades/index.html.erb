<!-- List decades from current to 1860’s -->
<!-- Retrieve release count for each decade based on artist start_year -->
<% @releases.info_by_decade.each do |decade, info| %>
  <br />
  <%= "#{decade}'s" %>
    <br />
  <!-- 'info' is a Release object -->
  <% info.each do |info| %>
  <!-- Calling 'album_info' on 'info' returns an Album object -->
    <% info.album_info.each do |album| %>
    <br />
      <img src="<%= album.album_image.cover_image %>" style="width:100px;height:100px">
      <br />
      <%= album.title %>
      <br />
      <%= album.artist.name %>
    <% end %>
  <% end %>
<% end %>


<!-- SIDEBAR NAVIGATION AREA BELOW -->

<!-- Retrieve info for first 3 genres ordered by most number of albums -->
<!-- Name -->
<!-- Count of albums associated with that genre -->
<% @genres.each do |genre| %>
  <br />
  <%= genre.name %>
  (<%= genre.album_count %>)
<% end %>

<!-- List decades from current to 1860’s -->
<!-- Retrieve release count for each decade based on artist start_year -->
<% @releases.count_by_decade.each do |decade, count| %>
  <br />
  <%= "#{decade}'s (#{count})" %>
<% end %>

<!-- List first 3 or 5 formats based either on count of releases associated with that format or most common format types -->
<!-- Retrieve album count associated with that format type -->
<% @formats.each do |format| %>
  <br />
  <%= format.name %>
  (<%= format.album_count %>)
<% end %>